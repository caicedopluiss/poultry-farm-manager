name: Terraform Infrastructure

on:
    workflow_call:
        inputs:
            terraform_action:
                description: "Terraform action to perform"
                required: true
                type: string
                # format, validate, plan, apply, destroy
            working_directory:
                description: "Terraform working directory"
                required: true
                type: string
            env_vars:
                description: "Additional environment variables (key=value format, newline-separated)"
                required: false
                type: string
                default: ""
            output_names:
                description: "Terraform output names to extract (newline-separated, only for apply action)"
                required: false
                type: string
                default: ""
            force_replace_resources:
                description: "Resources to force replace (newline-separated, e.g., digitalocean_app.platform)"
                required: false
                type: string
                default: ""
        secrets:
            DIGITALOCEAN_ACCESS_TOKEN:
                description: "DigitalOcean access token"
                required: false # No requerido para format
            TF_API_TOKEN:
                description: "HCP Terraform Cloud API token"
                required: false # No requerido para format
        outputs:
            extracted_outputs:
                description: "Extracted specific outputs as name=value pairs"
                value: ${{ jobs.terraform.outputs.extracted_outputs }}

jobs:
    terraform:
        runs-on: ubuntu-latest
        outputs:
            extracted_outputs: ${{ steps.get-terraform-outputs.outputs.extracted_outputs }}
        env:
            TF_VAR_digitalocean_token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

        defaults:
            run:
                working-directory: ${{ inputs.working_directory }}

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Terraform
              uses: hashicorp/setup-terraform@v3
              with:
                  terraform_version: "~1.13"
                  cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

            - name: Set additional environment variables
              if: inputs.env_vars != ''
              run: |
                  echo "Setting additional environment variables..."
                  while IFS= read -r line; do
                      if [[ -n "$line" && "$line" != \#* ]]; then
                          echo "$line" >> $GITHUB_ENV
                      fi
                  done <<< "${{ inputs.env_vars }}"
                  echo "✅ Additional environment variables set successfully"

            - name: Terraform Init
              if: inputs.terraform_action != 'format'
              run: terraform init

            - name: Terraform Format Check and Apply
              if: inputs.terraform_action == 'format'
              run: |
                  echo "🎨 Checking Terraform format..."

                  # Check format first
                  if terraform fmt -check -recursive; then
                      echo "✅ All Terraform files are properly formatted"
                  else
                      echo "⚠️ Some Terraform files need formatting, applying format..."
                      terraform fmt -recursive
                      echo "✅ Terraform formatting applied successfully"

                      # List formatted files
                      echo "📝 Files that were formatted:"
                      git diff --name-only || echo "No changes to show"
                  fi

            - name: Terraform Validate
              if: inputs.terraform_action == 'validate'
              run: |
                  echo "🔍 Validating Terraform configuration..."
                  terraform validate
                  echo "✅ Terraform configuration is valid"

            - name: Build replace flags
              if: (inputs.terraform_action == 'plan' || inputs.terraform_action == 'apply') && inputs.force_replace_resources != ''
              run: |
                  echo "🔧 Building resource replacement flags..."

                  # Use runner's isolated temp directory for security
                  REPLACE_ARGS_FILE="$RUNNER_TEMP/replace_args.txt"
                  rm -f "$REPLACE_ARGS_FILE"
                  touch "$REPLACE_ARGS_FILE"

                  while IFS= read -r resource; do
                      # Skip empty lines and comments
                      if [[ -n "$resource" && "$resource" != \#* ]]; then
                          # Trim whitespace
                          resource=$(echo "$resource" | xargs)
                          if [[ -n "$resource" ]]; then
                              echo "⚠️ Will replace: $resource"
                              # Write each argument on a new line for safe handling
                              echo "-replace=$resource" >> "$REPLACE_ARGS_FILE"
                          fi
                      fi
                  done <<< "${{ inputs.force_replace_resources }}"

                  # Store file path and flag in environment for subsequent steps
                  echo "REPLACE_ARGS_FILE=$REPLACE_ARGS_FILE" >> $GITHUB_ENV
                  if [[ -s "$REPLACE_ARGS_FILE" ]]; then
                      echo "HAS_REPLACE_FLAGS=true" >> $GITHUB_ENV
                      echo "✅ Replace flags configured"
                  else
                      echo "HAS_REPLACE_FLAGS=false" >> $GITHUB_ENV
                  fi

            - name: Terraform Plan
              if: inputs.terraform_action == 'plan' || inputs.terraform_action == 'apply'
              run: |
                  echo "📋 Showing Terraform plan..."

                  # Build args array safely
                  ARGS=("plan")

                  # Add replace flags if they exist
                  if [[ "$HAS_REPLACE_FLAGS" == "true" && -f "$REPLACE_ARGS_FILE" ]]; then
                      while IFS= read -r arg; do
                          ARGS+=("$arg")
                      done < "$REPLACE_ARGS_FILE"
                  fi

                  # Execute terraform with proper argument handling (no eval)
                  terraform "${ARGS[@]}"
                  echo "✅ Terraform plan displayed"

            - name: Terraform Apply
              if: inputs.terraform_action == 'apply'
              run: |
                  echo "🚀 Applying Terraform configuration..."

                  # Build args array safely
                  ARGS=("apply" "-auto-approve")

                  # Add replace flags if they exist
                  if [[ "$HAS_REPLACE_FLAGS" == "true" && -f "$REPLACE_ARGS_FILE" ]]; then
                      while IFS= read -r arg; do
                          ARGS+=("$arg")
                      done < "$REPLACE_ARGS_FILE"
                  fi

                  # Execute terraform with proper argument handling (no eval)
                  terraform "${ARGS[@]}"
                  echo "✅ Terraform apply completed successfully"
                  echo "### ✅ Terraform Apply - Completed" >> $GITHUB_STEP_SUMMARY
                  echo "Terraform configuration has been applied successfully." >> $GITHUB_STEP_SUMMARY

            - name: Terraform Destroy
              if: inputs.terraform_action == 'destroy'
              run: |
                  echo "🗑️ Destroying Terraform infrastructure..."
                  terraform destroy -auto-approve
                  echo "✅ Terraform destroy completed successfully"

            - name: Get Terraform Outputs
              if: (inputs.terraform_action == 'apply' && inputs.output_names != '')
              id: get-terraform-outputs
              run: |
                  echo "🎯 Getting Terraform outputs..."

                  OUTPUT_NAMES="${{ inputs.output_names }}"
                  EXTRACTED_OUTPUTS=""

                  if [[ -n "$OUTPUT_NAMES" ]]; then
                      echo "📋 Processing requested outputs..."

                      # Process each output name
                      while IFS= read -r output_name; do
                          # Skip empty lines and comments
                          if [[ -n "$output_name" && "$output_name" != \#* ]]; then
                              # Trim whitespace
                              output_name=$(echo "$output_name" | xargs)

                              echo "🔍 Checking output: $output_name"

                              # Check if output exists and get its value
                              if terraform output "$output_name" >/dev/null 2>&1; then
                                  OUTPUT_VALUE=$(terraform output -raw "$output_name")

                                  # Add to extracted outputs
                                  if [[ -z "$EXTRACTED_OUTPUTS" ]]; then
                                      EXTRACTED_OUTPUTS="${output_name}=${OUTPUT_VALUE}"
                                  else
                                      EXTRACTED_OUTPUTS="${EXTRACTED_OUTPUTS}
                                      ${output_name}=${OUTPUT_VALUE}"
                                  fi

                                  echo "✅ $output_name: $OUTPUT_VALUE"
                              else
                                  echo "⚠️ Output '$output_name' not found"

                                  # Add as N/A
                                  if [[ -z "$EXTRACTED_OUTPUTS" ]]; then
                                      EXTRACTED_OUTPUTS="${output_name}=N/A"
                                  else
                                      EXTRACTED_OUTPUTS="${EXTRACTED_OUTPUTS}
                                      ${output_name}=N/A"
                                  fi
                              fi
                          fi
                      done <<< "$OUTPUT_NAMES"

                      # Set the output for GitHub Actions
                      echo "extracted_outputs<<EOF" >> $GITHUB_OUTPUT
                      echo "$EXTRACTED_OUTPUTS" >> $GITHUB_OUTPUT
                      echo "EOF" >> $GITHUB_OUTPUT

                      # Add to step summary
                      echo "### 🎯 Extracted Terraform Outputs" >> $GITHUB_STEP_SUMMARY
                      echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
                      echo "$EXTRACTED_OUTPUTS" >> $GITHUB_STEP_SUMMARY
                      echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

                      echo "✅ Successfully extracted outputs"
                  else
                      echo "⚠️ No output names specified"
                      echo "extracted_outputs=" >> $GITHUB_OUTPUT
                  fi
